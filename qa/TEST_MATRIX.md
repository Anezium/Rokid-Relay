# Rokid Relay QA Matrix

Generated by `$rokid-qa-generator`. Treat this as a starting point; tighten expectations after adding debug hooks.

## Modules

| Module | Role | Package/Namespace | Key Signals | Tests |
| --- | --- | --- | --- | ---: |
| `glasses` | glasses | `com.anezium.rokidrelay.glasses` | accessibility, caps_protocol, cxr_s, input | 7 |
| `phone` | phone | `com.anezium.rokidrelay.phone` | caps_protocol, cxr_l_or_m, debug_hooks, helper_install, input, notifications, voice | 0 |

## Suggested Build Commands

- `.\gradlew.bat :glasses:testDebugUnitTest`
- `.\gradlew.bat :glasses:assembleDebug`
- `.\gradlew.bat :phone:assembleDebug`

## Scenarios

| ID | Risk | Automation Layer | Why It Matters |
| --- | --- | --- | --- |
| `app-launch-smoke` | high | device smoke | Build, install, launch, and capture baseline state |
| `input-alias-debounce` | high | unit + device | Physical swipe alias pair moves focus exactly once |
| `back-and-confirm-navigation` | medium | device smoke | Tap/confirm and back do not trap the HUD |
| `notification-direct-reply` | high | device smoke | Replyable notification reaches glasses and records reply result |
| `cxr-protocol-roundtrip` | high | unit + device | Phone/glasses protocol handles known and unknown messages |
| `helper-install-version-refresh` | high | device smoke | Phone update refreshes stale bundled glasses helper |
| `voice-capture-cancel-and-timeout` | high | unit + device | Voice capture cancels and times out without sending blank output |

## Missing Hooks

- Implement DUMP_STATE as one-line JSON in logcat or a pullable app-private file.
- Expose an input-trace recorder/replayer so real touchpad event sequences become unit fixtures.
- Add SEND_FAKE_CXR_MESSAGE to exercise protocol reducers without live CXR service.
- Expose bundled helper version/fingerprint and last-installed helper version in DUMP_STATE.
- Add fake voice-state/audio hooks for no-first-byte, silence, cancel, VAD end, and timeout.

## Next Actions

1. Implement missing debug hooks behind debug-only source sets or build flags.
2. Convert input/protocol/voice behavior into pure unit tests before relying on devices.
3. Replace placeholder scenario expectations with stable `DUMP_STATE` assertions.
4. Run `scripts/rokid-regression.ps1` with explicit ADB serials when multiple devices are connected.
